{% include 'nav-bread-crumbs' %}
<section class="uk-section uk-padding-remove-bottom">
  <div class="uk-container uk-padding uk-padding-remove-bottom">
    <h1 class="fg-text-shadow_collection-header uk-h1 fg-repsonsive-font">{{ page_title }}</h1>
  </div>
</section>
<section class="uk-section">
  <div class="uk-container">
    <div class="uk-section js-filter uk-child-width-1-1 uk-child-width-1-3@s uk-child-width-1-4@m uk-grid-small uk-grid-match" uk-grid>
      {% for collection in collections %}
        {% unless collection.handle == 'frontpage' %}
          {% include 'card-collection' %}
        {% endunless %}
      {% endfor %}
    </div>
  </div>
</section>
