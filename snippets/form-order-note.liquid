<fieldset class="uk-fieldset" id="cart-note">
  <legend class="uk-h3">Add a note to your order</legend>
  <textarea class="uk-textarea uk-margin" name="note" id="note" rows="5">{{ cart.note }}</textarea>
</fieldset>
