<img class="" data-src="{{ collection.image.src | img_url: '1920x' }}" data-srcset="{% include 'responsive-srcset-image', image: collection.image %}" 
data-sizes="auto" alt="{{ collection.image.alt | escape }}"
                data-aspectratio="{{ image.aspect_ratio }}"
    data-sizes="auto"
    tabindex="-1" uk-img>
